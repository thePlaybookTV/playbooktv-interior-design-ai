# Paperspace-specific requirements
# CUDA 12.1 compatible versions for Paperspace Gradient base images

# INSTALL INSTRUCTIONS:
# Step 1: Install PyTorch with CUDA 12.1
#   pip install --index-url https://download.pytorch.org/whl/cu121 torch==2.4.1 torchvision==0.19.1
# Step 2: Install other dependencies from regular PyPI
#   pip install ultralytics==8.0.0 "numpy<2" pillow python-dotenv duckdb pandas tqdm

# PyTorch versions (install from CUDA index)
torch==2.4.1
torchvision==0.19.1

# Other dependencies (install from regular PyPI)
ultralytics==8.0.0
pillow
python-dotenv
duckdb
pandas
tqdm
